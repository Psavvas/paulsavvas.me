name: CI

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

jobs:
  format-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Format code
        run: bun run format

      - name: Check for formatting changes
        id: verify_diff
        run: |
          git diff --quiet && git diff --staged --quiet || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit formatting changes
        if: steps.verify_diff.outputs.changed == 'true' && github.event_name == 'push' && github.event.repository.fork == false
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -A
          git commit -m "chore: auto-format code"

      - name: Push formatting changes
        if: steps.verify_diff.outputs.changed == 'true' && github.event_name == 'push' && github.event.repository.fork == false
        run: git push

      - name: Build
        run: bun run build

      - name: Verify build output
        run: |
          if [ ! -f "dist/index.html" ]; then
            echo "Error: Build failed - index.html not found in dist/"
            exit 1
          fi
          if [ ! -f "dist/.nojekyll" ]; then
            echo "Error: Build failed - .nojekyll not found in dist/"
            exit 1
          fi
          echo "✅ Build verification passed"
